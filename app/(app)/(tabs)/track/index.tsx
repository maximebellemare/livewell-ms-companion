import AppScreen from "../../../../components/ui/AppScreen";
import AppText from "../../../../components/ui/AppText";

export default function TrackScreen() {
  return (
    <AppScreen title="Track">
      <AppText>Authenticated tab shell only.</AppText>
    </AppScreen>
  );
}
